  | 容器               | 头文件               | 底层结构    | 特点            | 常用场景        |
| ---------------- | ----------------- | ------- | ------------- | ----------- |
| `vector`         | `<vector>`        | 动态数组    | 随机访问快、尾插快     | 最常用数组       |
| `string`         | `<string>`        | 字符数组    | 处理字符串         | 文本处理        |
| `array`          | `<array>`         | 固定数组    | STL版静态数组      | 很少竞赛用       |
| `deque`          | `<deque>`         | 双端数组    | 两端插入删除快       | 单调队列        |
| `queue`          | `<queue>`         | deque封装 | FIFO先进先出      | BFS         |
| `stack`          | `<stack>`         | deque封装 | LIFO后进先出      | 括号匹配、DFS模拟  |
| `priority_queue` | `<queue>`         | 堆       | 自动维护最大/最小值    | Dijkstra、贪心 |
| `set`            | `<set>`           | 红黑树     | 自动排序、去重       | 有序去重        |
| `multiset`       | `<set>`           | 红黑树     | 可重复排序         | 维护重复元素      |
| `map`            | `<map>`           | 红黑树     | key-value自动排序 | 字典映射        |
| `multimap`       | `<map>`           | 红黑树     | key可重复        | 较少用         |
| `unordered_set`  | `<unordered_set>` | 哈希表     | 查找极快、无序       | 哈希去重        |
| `unordered_map`  | `<unordered_map>` | 哈希表     | 哈希映射          | 统计频率        |
| `bitset`         | `<bitset>`        | 位压缩     | 极省空间          | 状压          |
| `list`           | `<list>`          | 双向链表    | 中间插删快         | 很少竞赛用       |
